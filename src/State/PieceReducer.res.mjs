// Generated by ReScript, PLEASE EDIT WITH CARE

import * as StateTypes from "./StateTypes.res.mjs";
import * as Stdlib_Int from "@rescript/runtime/lib/es6/Stdlib_Int.js";
import * as StateParity from "./StateParity.res.mjs";
import * as MoveExecutor from "../Move/MoveExecutor.res.mjs";
import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Primitive_exceptions from "@rescript/runtime/lib/es6/Primitive_exceptions.js";

let ReductionFailure = /* @__PURE__ */Primitive_exceptions.create("PieceReducer.ReductionFailure");

let cornerColours = [
  [
    "U",
    "R",
    "F"
  ],
  [
    "U",
    "F",
    "L"
  ],
  [
    "U",
    "L",
    "B"
  ],
  [
    "U",
    "B",
    "R"
  ],
  [
    "D",
    "F",
    "R"
  ],
  [
    "D",
    "L",
    "F"
  ],
  [
    "D",
    "B",
    "L"
  ],
  [
    "D",
    "R",
    "B"
  ]
];

let edgeColours = [
  [
    "U",
    "R"
  ],
  [
    "U",
    "F"
  ],
  [
    "U",
    "L"
  ],
  [
    "U",
    "B"
  ],
  [
    "D",
    "R"
  ],
  [
    "D",
    "F"
  ],
  [
    "D",
    "L"
  ],
  [
    "D",
    "B"
  ],
  [
    "F",
    "R"
  ],
  [
    "F",
    "L"
  ],
  [
    "B",
    "L"
  ],
  [
    "B",
    "R"
  ]
];

function cornerFacelets(size) {
  if (size !== 2) {
    if (size !== 3) {
      return {
        TAG: "Error",
        _0: {
          TAG: "UnsupportedSize",
          _0: size
        }
      };
    } else {
      return {
        TAG: "Ok",
        _0: [
          [
            [
              "U",
              8
            ],
            [
              "R",
              0
            ],
            [
              "F",
              2
            ]
          ],
          [
            [
              "U",
              6
            ],
            [
              "F",
              0
            ],
            [
              "L",
              2
            ]
          ],
          [
            [
              "U",
              0
            ],
            [
              "L",
              0
            ],
            [
              "B",
              2
            ]
          ],
          [
            [
              "U",
              2
            ],
            [
              "B",
              0
            ],
            [
              "R",
              2
            ]
          ],
          [
            [
              "D",
              2
            ],
            [
              "F",
              8
            ],
            [
              "R",
              6
            ]
          ],
          [
            [
              "D",
              0
            ],
            [
              "L",
              8
            ],
            [
              "F",
              6
            ]
          ],
          [
            [
              "D",
              6
            ],
            [
              "B",
              8
            ],
            [
              "L",
              6
            ]
          ],
          [
            [
              "D",
              8
            ],
            [
              "R",
              8
            ],
            [
              "B",
              6
            ]
          ]
        ]
      };
    }
  } else {
    return {
      TAG: "Ok",
      _0: [
        [
          [
            "U",
            3
          ],
          [
            "R",
            0
          ],
          [
            "F",
            1
          ]
        ],
        [
          [
            "U",
            2
          ],
          [
            "F",
            0
          ],
          [
            "L",
            1
          ]
        ],
        [
          [
            "U",
            0
          ],
          [
            "L",
            0
          ],
          [
            "B",
            1
          ]
        ],
        [
          [
            "U",
            1
          ],
          [
            "B",
            0
          ],
          [
            "R",
            1
          ]
        ],
        [
          [
            "D",
            1
          ],
          [
            "F",
            3
          ],
          [
            "R",
            2
          ]
        ],
        [
          [
            "D",
            0
          ],
          [
            "L",
            3
          ],
          [
            "F",
            2
          ]
        ],
        [
          [
            "D",
            2
          ],
          [
            "B",
            3
          ],
          [
            "L",
            2
          ]
        ],
        [
          [
            "D",
            3
          ],
          [
            "R",
            3
          ],
          [
            "B",
            2
          ]
        ]
      ]
    };
  }
}

let edgeFacelets = [
  [
    [
      "U",
      5
    ],
    [
      "R",
      1
    ]
  ],
  [
    [
      "U",
      7
    ],
    [
      "F",
      1
    ]
  ],
  [
    [
      "U",
      3
    ],
    [
      "L",
      1
    ]
  ],
  [
    [
      "U",
      1
    ],
    [
      "B",
      1
    ]
  ],
  [
    [
      "D",
      5
    ],
    [
      "R",
      7
    ]
  ],
  [
    [
      "D",
      1
    ],
    [
      "F",
      7
    ]
  ],
  [
    [
      "D",
      3
    ],
    [
      "L",
      7
    ]
  ],
  [
    [
      "D",
      7
    ],
    [
      "B",
      7
    ]
  ],
  [
    [
      "F",
      5
    ],
    [
      "R",
      3
    ]
  ],
  [
    [
      "F",
      3
    ],
    [
      "L",
      5
    ]
  ],
  [
    [
      "B",
      5
    ],
    [
      "L",
      3
    ]
  ],
  [
    [
      "B",
      3
    ],
    [
      "R",
      5
    ]
  ]
];

function describeError(error) {
  if (typeof error !== "object") {
    return "Corner and edge permutations must have matching parity.";
  }
  switch (error.TAG) {
    case "UnsupportedSize" :
      let label = error._0.toString();
      return `Piece coordinates are unavailable for ` + label + `×` + label + `×` + label + `.`;
    case "SolvabilityViolation" :
      return StateParity.describe(error._0);
    default:
      return error._0;
  }
}

function getFacelet(state, param) {
  let values = state.facelets[StateTypes.storageIndex(param[0])];
  return values[param[1]];
}

function setFacelet(state, param, value) {
  let values = state.facelets[StateTypes.storageIndex(param[0])];
  values[param[1]] = value;
}

function validateStateShape(state) {
  if (state.size !== 2 && state.size !== 3) {
    throw {
      RE_EXN_ID: ReductionFailure,
      _1: {
        TAG: "UnsupportedSize",
        _0: state.size
      },
      Error: new Error()
    };
  }
  if (state.facelets.length !== 6) {
    throw {
      RE_EXN_ID: ReductionFailure,
      _1: {
        TAG: "InvalidFaceletState",
        _0: "A cube state must contain exactly six faces."
      },
      Error: new Error()
    };
  }
  let expected = state.size * state.size | 0;
  state.facelets.forEach(values => {
    if (values.length === expected) {
      return;
    }
    throw {
      RE_EXN_ID: ReductionFailure,
      _1: {
        TAG: "InvalidFaceletState",
        _0: `Every face must contain exactly ` + expected.toString() + ` facelets.`
      },
      Error: new Error()
    };
  });
}

function centresAreCanonical(state) {
  let centre = (state.size * state.size | 0) / 2 | 0;
  return StateTypes.storageOrder.every(face => getFacelet(state, [
    face,
    centre
  ]) === face);
}

function rotateTimes(state, move, turns) {
  let result = state;
  for (let _for = 1; _for <= turns; ++_for) {
    result = MoveExecutor.applyQuarter(result, move, 1);
  }
  return result;
}

function normalizeCentres(state) {
  if (centresAreCanonical(state)) {
    return state;
  }
  let normalized;
  for (let xTurns = 0; xTurns <= 3; ++xTurns) {
    let afterX = rotateTimes(state, {
      TAG: "Rotation",
      _0: "X"
    }, xTurns);
    for (let yTurns = 0; yTurns <= 3; ++yTurns) {
      let afterY = rotateTimes(afterX, {
        TAG: "Rotation",
        _0: "Y"
      }, yTurns);
      for (let zTurns = 0; zTurns <= 3; ++zTurns) {
        let candidate = rotateTimes(afterY, {
          TAG: "Rotation",
          _0: "Z"
        }, zTurns);
        if (normalized === undefined && centresAreCanonical(candidate)) {
          normalized = candidate;
        }
      }
    }
  }
  let state$1 = normalized;
  if (state$1 !== undefined) {
    return state$1;
  }
  throw {
    RE_EXN_ID: ReductionFailure,
    _1: {
      TAG: "InvalidCenters",
      _0: "The six centre facelets do not define a valid cube orientation."
    },
    Error: new Error()
  };
}

function validatePermutation(name, permutation, expectedLength) {
  if (permutation.length !== expectedLength) {
    throw {
      RE_EXN_ID: ReductionFailure,
      _1: {
        TAG: "InvalidPermutation",
        _0: name + ` must contain exactly ` + expectedLength.toString() + ` values.`
      },
      Error: new Error()
    };
  }
  let seen = Stdlib_Array.make(expectedLength, false);
  permutation.forEach(value => {
    if (value < 0 || value >= expectedLength || seen[value]) {
      throw {
        RE_EXN_ID: ReductionFailure,
        _1: {
          TAG: "InvalidPermutation",
          _0: name + ` must be a permutation of 0 through ` + (expectedLength - 1 | 0).toString() + `.`
        },
        Error: new Error()
      };
    }
    seen[value] = true;
  });
}

function validateOrientations(name, orientations, expectedLength, modulus) {
  if (orientations.length !== expectedLength) {
    throw {
      RE_EXN_ID: ReductionFailure,
      _1: {
        TAG: "InvalidOrientation",
        _0: name + ` must contain exactly ` + expectedLength.toString() + ` values.`
      },
      Error: new Error()
    };
  }
  orientations.forEach(value => {
    if (!(value < 0 || value >= modulus)) {
      return;
    }
    throw {
      RE_EXN_ID: ReductionFailure,
      _1: {
        TAG: "InvalidOrientation",
        _0: name + ` values must be between 0 and ` + (modulus - 1 | 0).toString() + `.`
      },
      Error: new Error()
    };
  });
}

function validateOrThrow(pieces) {
  if (pieces.size !== 2 && pieces.size !== 3) {
    throw {
      RE_EXN_ID: ReductionFailure,
      _1: {
        TAG: "UnsupportedSize",
        _0: pieces.size
      },
      Error: new Error()
    };
  }
  validatePermutation("cp", pieces.cp, 8);
  validateOrientations("co", pieces.co, 8, 3);
  if (pieces.size === 2) {
    if (pieces.ep.length !== 0 || pieces.eo.length !== 0) {
      throw {
        RE_EXN_ID: ReductionFailure,
        _1: {
          TAG: "InvalidPiece",
          _0: "A 2×2×2 piece state cannot contain edge coordinates."
        },
        Error: new Error()
      };
    }
  } else {
    validatePermutation("ep", pieces.ep, 12);
    validateOrientations("eo", pieces.eo, 12, 2);
  }
  let violation = StateParity.validate({
    size: pieces.size,
    cp: pieces.cp,
    co: pieces.co,
    ep: pieces.ep,
    eo: pieces.eo
  });
  if (violation.TAG === "Ok") {
    return;
  }
  throw {
    RE_EXN_ID: ReductionFailure,
    _1: {
      TAG: "SolvabilityViolation",
      _0: violation._0
    },
    Error: new Error()
  };
}

function validate(pieces) {
  try {
    validateOrThrow(pieces);
    return {
      TAG: "Ok",
      _0: undefined
    };
  } catch (raw_error) {
    let error = Primitive_exceptions.internalToException(raw_error);
    if (error.RE_EXN_ID === ReductionFailure) {
      return {
        TAG: "Error",
        _0: error._1
      };
    }
    throw error;
  }
}

function identifyCorners(state, locations) {
  return locations.map((slotFacelets, slot) => {
    let orientation = -1;
    for (let index = 0; index <= 2; ++index) {
      let colour = getFacelet(state, slotFacelets[index]);
      if (colour === "U" || colour === "D") {
        orientation = index;
      }
    }
    if (orientation === -1) {
      throw {
        RE_EXN_ID: ReductionFailure,
        _1: {
          TAG: "InvalidPiece",
          _0: `Corner slot ` + slot.toString() + ` has no U or D facelet.`
        },
        Error: new Error()
      };
    }
    let second = getFacelet(state, slotFacelets[(orientation + 1 | 0) % 3]);
    let third = getFacelet(state, slotFacelets[(orientation + 2 | 0) % 3]);
    let piece = -1;
    for (let candidate = 0; candidate <= 7; ++candidate) {
      let colours = cornerColours[candidate];
      let first = getFacelet(state, slotFacelets[orientation]);
      if (first === colours[0] && second === colours[1] && third === colours[2]) {
        piece = candidate;
      }
    }
    if (piece === -1) {
      throw {
        RE_EXN_ID: ReductionFailure,
        _1: {
          TAG: "InvalidPiece",
          _0: `Corner slot ` + slot.toString() + ` contains an unknown colour combination.`
        },
        Error: new Error()
      };
    }
    return [
      piece,
      orientation
    ];
  });
}

function identifyEdges(state) {
  return edgeFacelets.map((slotFacelets, slot) => {
    let first = getFacelet(state, slotFacelets[0]);
    let second = getFacelet(state, slotFacelets[1]);
    let piece = -1;
    let orientation = -1;
    for (let candidate = 0; candidate <= 11; ++candidate) {
      let colours = edgeColours[candidate];
      if (first === colours[0] && second === colours[1]) {
        piece = candidate;
        orientation = 0;
      } else if (first === colours[1] && second === colours[0]) {
        piece = candidate;
        orientation = 1;
      }
    }
    if (piece === -1) {
      throw {
        RE_EXN_ID: ReductionFailure,
        _1: {
          TAG: "InvalidPiece",
          _0: `Edge slot ` + slot.toString() + ` contains an unknown colour combination.`
        },
        Error: new Error()
      };
    }
    return [
      piece,
      orientation
    ];
  });
}

function reduce(input) {
  try {
    validateStateShape(input);
    let state = input.size === 3 ? normalizeCentres(input) : input;
    let locations = cornerFacelets(state.size);
    let locations$1;
    if (locations.TAG === "Ok") {
      locations$1 = locations._0;
    } else {
      throw {
        RE_EXN_ID: ReductionFailure,
        _1: locations._0,
        Error: new Error()
      };
    }
    let corners = identifyCorners(state, locations$1);
    let match;
    if (state.size === 3) {
      let edges = identifyEdges(state);
      match = [
        edges.map(param => param[0]),
        edges.map(param => param[1])
      ];
    } else {
      match = [
        [],
        []
      ];
    }
    let pieces_size = state.size;
    let pieces_cp = corners.map(param => param[0]);
    let pieces_co = corners.map(param => param[1]);
    let pieces_ep = match[0];
    let pieces_eo = match[1];
    let pieces = {
      size: pieces_size,
      cp: pieces_cp,
      co: pieces_co,
      ep: pieces_ep,
      eo: pieces_eo
    };
    validateOrThrow(pieces);
    return {
      TAG: "Ok",
      _0: pieces
    };
  } catch (raw_error) {
    let error = Primitive_exceptions.internalToException(raw_error);
    if (error.RE_EXN_ID === ReductionFailure) {
      return {
        TAG: "Error",
        _0: error._1
      };
    }
    throw error;
  }
}

function reconstruct(pieces) {
  try {
    validateOrThrow(pieces);
    let state = StateTypes.solved(pieces.size);
    let state$1;
    if (state.TAG === "Ok") {
      state$1 = state._0;
    } else {
      throw {
        RE_EXN_ID: ReductionFailure,
        _1: {
          TAG: "UnsupportedSize",
          _0: pieces.size
        },
        Error: new Error()
      };
    }
    let locations = cornerFacelets(pieces.size);
    let locations$1;
    if (locations.TAG === "Ok") {
      locations$1 = locations._0;
    } else {
      throw {
        RE_EXN_ID: ReductionFailure,
        _1: locations._0,
        Error: new Error()
      };
    }
    for (let slot = 0; slot <= 7; ++slot) {
      let slotFacelets = locations$1[slot];
      let colours = cornerColours[pieces.cp[slot]];
      let orientation = pieces.co[slot];
      for (let colourIndex = 0; colourIndex <= 2; ++colourIndex) {
        let target = slotFacelets[(colourIndex + orientation | 0) % 3];
        setFacelet(state$1, target, colours[colourIndex]);
      }
    }
    if (pieces.size === 3) {
      for (let slot$1 = 0; slot$1 <= 11; ++slot$1) {
        let slotFacelets$1 = edgeFacelets[slot$1];
        let colours$1 = edgeColours[pieces.ep[slot$1]];
        let orientation$1 = pieces.eo[slot$1];
        for (let colourIndex$1 = 0; colourIndex$1 <= 1; ++colourIndex$1) {
          let target$1 = slotFacelets$1[(colourIndex$1 + orientation$1 | 0) % 2];
          setFacelet(state$1, target$1, colours$1[colourIndex$1]);
        }
      }
    }
    return {
      TAG: "Ok",
      _0: state$1
    };
  } catch (raw_error) {
    let error = Primitive_exceptions.internalToException(raw_error);
    if (error.RE_EXN_ID === ReductionFailure) {
      return {
        TAG: "Error",
        _0: error._1
      };
    }
    throw error;
  }
}

function renderValues(values) {
  return values.map(value => value.toString()).join(" ");
}

function render(pieces) {
  let error = validate(pieces);
  if (error.TAG !== "Ok") {
    return {
      TAG: "Error",
      _0: error._0
    };
  }
  let corners = `cp: ` + renderValues(pieces.cp) + `; co: ` + renderValues(pieces.co);
  if (pieces.size === 2) {
    return {
      TAG: "Ok",
      _0: corners
    };
  } else {
    return {
      TAG: "Ok",
      _0: corners + `; ep: ` + renderValues(pieces.ep) + `; eo: ` + renderValues(pieces.eo)
    };
  }
}

function parseField(field, expectedLabel) {
  let parts = field.split(":");
  if (parts.length !== 2 || parts[0].trim() !== expectedLabel) {
    throw {
      RE_EXN_ID: ReductionFailure,
      _1: {
        TAG: "InvalidSyntax",
        _0: `Expected the '` + expectedLabel + `:' coordinate field in canonical order.`
      },
      Error: new Error()
    };
  }
  let valueText = parts[1].trim();
  if (valueText === "") {
    return [];
  } else {
    return valueText.split(" ").filter(value => value !== "").map(value => {
      let value$1 = Stdlib_Int.fromString(value, undefined);
      if (value$1 !== undefined) {
        return value$1;
      }
      throw {
        RE_EXN_ID: ReductionFailure,
        _1: {
          TAG: "InvalidSyntax",
          _0: `'` + value + `' is not a valid integer in the '` + expectedLabel + `' field.`
        },
        Error: new Error()
      };
    });
  }
}

function parse(size, input) {
  try {
    if (size !== 2 && size !== 3) {
      throw {
        RE_EXN_ID: ReductionFailure,
        _1: {
          TAG: "UnsupportedSize",
          _0: size
        },
        Error: new Error()
      };
    }
    let fields = input.trim().split(";");
    let expectedFields = size === 2 ? 2 : 4;
    if (fields.length !== expectedFields) {
      throw {
        RE_EXN_ID: ReductionFailure,
        _1: {
          TAG: "InvalidSyntax",
          _0: `A ` + size.toString() + `×` + size.toString() + ` cubie state requires ` + expectedFields.toString() + ` coordinate fields.`
        },
        Error: new Error()
      };
    }
    let pieces_cp = parseField(fields[0], "cp");
    let pieces_co = parseField(fields[1], "co");
    let pieces_ep = size === 3 ? parseField(fields[2], "ep") : [];
    let pieces_eo = size === 3 ? parseField(fields[3], "eo") : [];
    let pieces = {
      size: size,
      cp: pieces_cp,
      co: pieces_co,
      ep: pieces_ep,
      eo: pieces_eo
    };
    validateOrThrow(pieces);
    return {
      TAG: "Ok",
      _0: pieces
    };
  } catch (raw_error) {
    let error = Primitive_exceptions.internalToException(raw_error);
    if (error.RE_EXN_ID === ReductionFailure) {
      return {
        TAG: "Error",
        _0: error._1
      };
    }
    throw error;
  }
}

function parseState(size, input) {
  let error = parse(size, input);
  if (error.TAG === "Ok") {
    return reconstruct(error._0);
  } else {
    return {
      TAG: "Error",
      _0: error._0
    };
  }
}

export {
  ReductionFailure,
  cornerColours,
  edgeColours,
  cornerFacelets,
  edgeFacelets,
  describeError,
  getFacelet,
  setFacelet,
  validateStateShape,
  centresAreCanonical,
  rotateTimes,
  normalizeCentres,
  validatePermutation,
  validateOrientations,
  validateOrThrow,
  validate,
  identifyCorners,
  identifyEdges,
  reduce,
  reconstruct,
  renderValues,
  render,
  parseField,
  parse,
  parseState,
}
/* No side effect */
